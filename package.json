{
  "name": "tauri-downloader",
  "private": true,
  "version": "0.1.4",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc --noEmit && vite build",
    "check:versions": "node scripts/check-version.mjs",
    "preview": "vite preview",
    "test": "vitest run",
    "package:extension": "node scripts/package-extension.mjs all",
    "verify:extension": "node scripts/verify-extension.mjs",
    "test:watch": "vitest",
    "tauri": "tauri"
  },
  "dependencies": {
    "vue": "^3.5.13",
    "@tauri-apps/api": "^2"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.1",
    "typescript": "~5.6.2",
    "vite": "^6.0.3",
    "vitest": "^3.2.4",
    "vue-tsc": "^2.1.10",
    "@tauri-apps/cli": "^2"
  }
}
